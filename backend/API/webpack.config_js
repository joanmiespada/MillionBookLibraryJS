var path = require('path');

module.exports = {
  name: 'server',
  entry: ['babel-polyfill','./src/index_js'],
  target: 'node',
  output: {
    filename: 'API.js',
    path: path.join(process.cwd(), 'build', 'script')
  }
};
